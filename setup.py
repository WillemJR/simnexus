from setuptools import setup, find_packages

setup(
    name='simuflow',
    version='0.1.0',
    description='A Python module for modelling complex simulation workflows.',
    author='Willem',
    author_email='willem@example.com',
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here
    ],
)
